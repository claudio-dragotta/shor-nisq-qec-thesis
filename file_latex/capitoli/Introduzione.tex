\begin{onehalfspace}

\section{Il Limite del Calcolo Classico}

Per oltre cinquant'anni, lo sviluppo dell'informatica è stato guidato da una legge empirica enunciata nel 1965 da Gordon Moore\footnote{\textbf{Gordon Earle Moore} (1929--2023), cofondatore di Intel, osservò nel 1965 che il numero di transistor integrabili su un chip raddoppiava approssimativamente ogni due anni, con un conseguente aumento esponenziale delle prestazioni computazionali. La Legge di Moore non è una legge fisica, ma una previsione industriale che ha orientato decenni di investimenti tecnologici.}: il numero di transistor per chip raddoppia ogni due anni, e con esso la potenza di calcolo disponibile. Questa crescita esponenziale ha permesso di trasformare calcolatori grandi quanto un'intera stanza nei dispositivi tascabili di oggi. Tuttavia, la Legge di Moore si sta scontrando con i limiti fisici fondamentali della materia: i transistor moderni hanno dimensioni di pochi nanometri, avvicinandosi alla scala atomica oltre la quale gli effetti quantistici -- lo stesso fenomeno che si vorrebbe evitare -- diventano incontrollabili e inevitabili.

Parallelamente al problema fisico dell'hardware, esiste una barriera di natura puramente matematica: esistono classi di problemi computazionali per i quali nessun algoritmo classico è in grado di fornire una soluzione in tempi ragionevoli, \textit{indipendentemente} dalla potenza di calcolo disponibile. Questi problemi -- fattorizzazione di grandi interi, ricerca in spazi combinatoriali enormi, simulazione di sistemi molecolari complessi -- resistono a qualunque approccio classico non perché i computer non siano abbastanza veloci, ma perché la complessità intrinseca del problema cresce più rapidamente di qualunque risorsa classica concepibile.

È in questo contesto che nasce il \textbf{calcolo quantistico}: la visione radicale che i principi della meccanica quantistica -- \textit{sovrapposizione}, \textit{entanglement} e \textit{interferenza} -- possano essere sfruttati non come ostacoli da evitare, ma come risorse computazionali di straordinaria potenza. L'intuizione originale fu di Richard Feynman\footnote{\textbf{Richard Phillips Feynman} (1918--1988), fisico teorico statunitense, Premio Nobel per la Fisica nel 1965. Nel suo celebre articolo del 1982 \textit{``Simulating Physics with Computers''} \cite{feynman1982}, Feynman argomentò che un computer classico non può simulare efficientemente un sistema quantistico senza un overhead esponenziale, e propose che un ``computer quantistico'' -- uno che segua le leggi della meccanica quantistica -- potrebbe farlo in modo naturalmente efficiente.}, che nel 1982 osservò come la simulazione di sistemi quantistici richiedesse risorse esponenziali su macchine classiche, e propose che un computer basato sugli stessi principi fisici potesse simulare la natura stessa in modo efficiente \cite{feynman1982}. David Deutsch formalizzò nel 1985 il primo modello teorico rigoroso di computer quantistico universale \cite{deutsch1985}, aprendo la strada a un nuovo paradigma computazionale.

I principi fisici che rendono possibile questo paradigma --- il qubit e la sovrapposizione, la sfera di Bloch, l'entanglement e l'interferenza --- insieme alla loro realizzazione sui qubit superconduttori di IBM, sono ripresi nella Sezione~\ref{app:introduzione} (Sez.~\ref{app:intro:qubit}); la trattazione formale completa è nella Sezione~\ref{chap:fondamenti}. La rivoluzione algoritmica degli anni Novanta --- da Simon fino all'algoritmo di Shor (1994) e a quello di Grover (1996) --- è ripercorsa nella Sez.~\ref{app:intro:algoritmi}. La portata dirompente di Shor si misura sulla crittografia che minaccia: la struttura del protocollo RSA e il motivo per cui Shor lo rompe (Sez.~\ref{app:intro:rsa}), la risposta della crittografia post-quantistica con gli standard NIST 2024 (Sez.~\ref{app:intro:pqc}) e lo stato dell'arte dell'hardware dall'era NISQ verso i sistemi fault-tolerant (Sez.~\ref{app:intro:nisq}) delineano il contesto strategico in cui si colloca questo lavoro: comprendere i limiti reali dell'algoritmo di Shor su hardware rumoroso.
% -----------------------------------------------
\section{Motivazione e Contributo del Presente Lavoro}

In questo contesto scientifico e tecnologico si inserisce il presente lavoro di tesi. L'algoritmo di Shor rappresenta uno dei risultati più significativi e dirompenti dell'informatica quantistica: la sua capacità di fattorizzare interi in tempo polinomiale minaccia direttamente le fondamenta della crittografia moderna. Tuttavia, la sua esecuzione su hardware NISQ è ostacolata da requisiti stringenti in termini di profondità del circuito, numero di qubit e fedeltà delle porte -- rendendo il rumore quantistico il principale ostacolo alla sua realizzazione pratica.

Questo lavoro affronta tale sfida in due fasi. La \textbf{prima fase} è una verifica sperimentale sistematica: eseguire l'algoritmo di Shor su simulatori quantistici con due preset di rumore uniformi e illustrativi e mettere alla prova l'ipotesi che un classificatore \ac{ML} applicato all'output rumoroso riduca il numero di iterazioni necessarie. I preset non costituiscono la calibrazione di una specifica QPU. La verifica confronta tre configurazioni:
\begin{itemize}
    \item \textbf{Metodo 1} (baseline classico, TOP-1): la misura più frequente dell'istogramma viene usata come candidato. Richiede in media $\bar{M}_1=1.37$ esecuzioni nello scenario moderato e $1.50$ in quello di stress.
    \item \textbf{Metodo 2} (classificatore \ac{ML} TOP-1 + TOP-4): una \ac{SVM}, selezionata su validation e valutata su un test set indipendente, filtra gli istogrammi prima della ricerca sui quattro candidati più frequenti. Ottiene $\bar{M}_2=1.50$ e $1.37$ nei due scenari, senza un miglioramento significativo rispetto a M1.
    \item \textbf{Analisi di ablazione} ($M_{\text{TOP4}}$, TOP-4 senza classificatore): isola il contributo della ricerca multi-candidato. TOP-4 ottiene $\bar{M}=1.00$ in entrambi gli scenari ed è significativamente migliore della variante con filtro ($p_{\mathrm{Holm}}=0.0033$ e $0.0096$). L'audit delle etichette (Sezione~\ref{app:classificatore}) mostra inoltre che TOP-16 produce $2\,000/2\,000$ positivi: il problema documentato è a classe unica, mentre TOP-1 apprende soltanto la riuscita del candidato dominante.
\end{itemize}

L'esito della verifica --- l'\ac{ML} applicato all'output, in quel punto e in quel modo, non aggiunge valore alla regola TOP-4 --- delimita con precisione la portata del post-processing. L'istogramma conserva candidati utili, come mostra TOP-4, ma nessuna regola a valle può correggere gli errori che hanno già cancellato o spostato fuori dall'insieme osservato la struttura necessaria. Per intervenire su questi ultimi occorre proteggere il calcolo \emph{durante} l'esecuzione. La \textbf{seconda fase}, che costituisce il nucleo della tesi, è quindi dedicata alla \ac{QEC}: la costruzione e la simulazione dei codici correttori --- dal codice a ripetizione al codice di Steane $[[7,1,3]]$, fino al surface code con decodifica \ac{MWPM} --- e la misura sperimentale del \textbf{logical error rate} $p_L$ in funzione dell'errore fisico $p$ e della distanza di codice $d$. L'integrazione finale riusa il simbolo $p_L$ per un parametro fenomenologico applicato ai gate compilati inclusi nel proxy e svolge un'analisi di sensibilità di Shor; non identifica direttamente tale parametro con il tasso di fallimento di un memory experiment.

È in questa seconda fase che l'intelligenza artificiale viene ripresa, in un punto che espone direttamente informazione diagnostica: non più sull'istogramma finale, ma sulla \textbf{sindrome}. Un decoder appreso viene messo a confronto con il \ac{MWPM} sugli stessi campioni, e il risultato precisa il criterio che regge l'intero lavoro. Sostituire il decoder analitico non conviene: a parità di modello di rumore la rete lo pareggia soltanto, e a costo di volumi di dati considerevoli. Affiancarglielo sì: un decoder \textbf{ibrido}, in cui la rete apprende unicamente \emph{quando ribaltare} la decisione del matching, riduce l'errore logico fino al $21\%$ nel modello simulato con errori correlati che il grafo del decoder analitico non rappresenta --- e ricade su di esso, entro la risoluzione dell'esperimento, quando il rumore è quello previsto. L'apprendimento automatico, dunque, non ottimizza l'esecuzione di Shor ovunque lo si applichi: può farlo dove dispone di informazione strutturale che il metodo di riferimento non sfrutta, e questa tesi ne delimita sperimentalmente un caso d'uso.

L'elaborato si sviluppa dalle fondamenta teoriche del calcolo quantistico, attraverso la verifica sperimentale su simulatori rumorosi, fino alla correzione d'errore quantistica e allo Shor logico. Gli obiettivi specifici e la struttura dettagliata del lavoro sono descritti nel Capitolo~\ref{chap:obiettivi}.

\medskip
\noindent\textbf{Riproducibilità e materiale supplementare.} Tutto il lavoro condotto in questa tesi --- i sorgenti \LaTeX{}, il codice sperimentale e le campagne di simulazione con i relativi risultati (seed e parametri inclusi) --- è documentato ed è liberamente consultabile nel repository \textsc{GitHub} pubblico dell'autore: \url{https://github.com/claudio-dragotta/shor-nisq-qec-thesis}. È inoltre disponibile una \textbf{demo interattiva} dell'algoritmo di Shor --- circuito e sfere di Bloch passo per passo, con esecuzione statistica su molte iterazioni --- consultabile dal vivo all'indirizzo \url{https://shor-demo-6knp.onrender.com} (codice sorgente: \url{https://github.com/claudio-dragotta/shor-demo}).

\clearpage
% ⚠ SYNC-STRUTTURA: questa sezione deve essere aggiornata ogni volta che
%   si aggiunge, elimina o rinomina un capitolo. Quando modifichi un capitolo,
%   controlla se la sua descrizione qui è ancora accurata.
\section{Struttura della Tesi}

L'elaborato è organizzato in dodici capitoli, seguiti da cinque appendici (A--E) che raccolgono il contesto esteso, il materiale di dettaglio richiamato in forma sintetica nel corpo e un'esplorazione condotta oltre il perimetro del lavoro principale. Il Capitolo~2 svolge una funzione trasversale di raccordo tra teoria e pratica; i Capitoli~3--5 costituiscono la parte teorica; i Capitoli~6--10 la parte sperimentale; i Capitoli~11 e~12 chiudono il lavoro.

\medskip
Il \textbf{Capitolo~\ref{chap:obiettivi}} (\textit{Obiettivi e Piano Sperimentale}) motiva la vulnerabilità di RSA all'algoritmo di Shor e fissa il contratto sperimentale: obiettivi, ipotesi di ricerca, contributo, use case e metriche.

\medskip
\textbf{Parte teorica (Capitoli 3--5).}

Il \textbf{Capitolo~\ref{chap:shor}} (\textit{L'Algoritmo di Shor}) si apre con i richiami di calcolo quantistico necessari a seguirlo (Sezione~\ref{chap:fondamenti}: qubit, sovrapposizione, porte e circuiti, e l'entanglement come \textit{risorsa fisica fragile} che anticipa la trattazione del rumore), quindi presenta la riduzione al \textit{period finding}, la Quantum Phase Estimation e la complessità polinomiale $O((\log N)^3)$, con le implicazioni per RSA e la transizione post-quantum.

Il \textbf{Capitolo~\ref{chap:rumore}} (\textit{Rumore Quantistico nei Sistemi NISQ}) analizza le quattro fonti fisiche di errore e i canali di rumore fondamentali, fino a un proxy di nessun evento pari a circa $3{,}4\%$ nell'esempio illustrativo con lunghezza $n=15$ bit; tale proxy non coincide con la probabilità di successo di Shor.

Il \textbf{Capitolo~\ref{chap:strategie}} (\textit{Strategie per la Riduzione del Rumore Quantistico}) confronta le quattro macro-strategie anti-rumore nell'era NISQ e motiva l'approccio adottato.

\medskip
\textbf{Parte sperimentale (Capitoli 6--10).}

Il \textbf{Capitolo~\ref{chap:metodologia}} (\textit{Metodo e Implementazione}) risponde a tre domande in sequenza: con quali strumenti si lavora (Sezione~\ref{chap:strumenti}: Qiskit, il simulatore Aer su CPU, la valutazione non riuscita del backend GPU, il noise model parametrizzato e la transpilazione), con quale disegno sperimentale --- la pipeline a tre stadi, le tecniche di mitigazione e i due metodi confrontati --- e come il disegno è stato realizzato in codice, dall'ambiente WSL2 alla QFT inversa, dal circuito di Shor alla pipeline dei due metodi.

Il \textbf{Capitolo~\ref{chap:risultati}} (\textit{Verifica Sperimentale dell'Ipotesi Iniziale: dal Classificatore ML alla Strategia TOP-K}) stabilisce la baseline e, tramite analisi di ablazione, dimostra che il guadagno è della strategia TOP-4 e non del classificatore \ac{ML} --- il risultato che motiva il pivot alla QEC.

Il \textbf{Capitolo~\ref{chap:qec}} (\textit{La Correzione d'Errore Quantistico: dal Codice a Ripetizione al Codice di Steane}) presenta il ciclo di correzione e lo verifica sul codice a ripetizione e sul codice di Steane $[[7,1,3]]$.

Il \textbf{Capitolo~\ref{chap:surface}} (\textit{Il Surface Code e la Stima dell'Errore Logico}) estende la correzione al surface code e ne misura sperimentalmente l'errore logico e la soglia con Stim e PyMatching; le due sezioni conclusive mettono alla prova le assunzioni su cui quella stima poggia --- il modello di rumore con cui il codice è simulato e quello con cui è decodificato --- ed è nella seconda che il confronto fra decodifica appresa e \ac{MWPM} individua il regime in cui l'apprendimento automatico produce un guadagno misurabile.

Il \textbf{Capitolo~\ref{chap:integrazione}} (\textit{Analisi di Sensibilità: Shor sotto un Proxy di Errore per Gate}) misura la sensibilità di Shor a un proxy Pauli uniforme per gate compilato e spiega perché, senza una compilazione fault-tolerant, tale curva non può essere tradotta direttamente in requisiti hardware.

Il \textbf{Capitolo~\ref{chap:sviluppi}} (\textit{Sviluppi Futuri}) delinea le direzioni di ricerca oltre il perimetro sperimentale della tesi.

Il \textbf{Capitolo~\ref{chap:conclusioni}} (\textit{Conclusioni}) verifica una per una le sei ipotesi e le sei domande di ricerca fissate nel Capitolo~\ref{chap:obiettivi}, dichiara i contributi e i limiti del lavoro e isola il criterio che unifica le due fasi sperimentali.

\medskip
\textbf{Appendici.} Il corpo dell'elaborato è tenuto alla lunghezza di un argomento continuo; le cinque appendici raccolgono ciò che quel filo richiama ma non deve interrompere. Sono organizzate per tema, non per capitolo di provenienza, così che chi le consulti trovi in un solo luogo tutto il materiale di una stessa natura.

L'\textbf{Appendice~\ref{app:teoria}} (\textit{Trattazione Teorica Estesa}) sviluppa per esteso la teoria sintetizzata nei Capitoli~\ref{chap:shor}--\ref{chap:rumore}: i postulati della meccanica quantistica e l'insieme completo delle porte (Sez.~\ref{app:fondamenti}), l'anatomia fisica di una \ac{QPU} e il confronto fra le tecnologie di qubit (Sez.~\ref{app:anatomia}), gli approfondimenti sull'algoritmo di Shor e sullo stato dell'arte della fattorizzazione quantistica (Sez.~\ref{app:shor_esteso}) e le derivazioni complete dei canali di rumore in forma di Kraus (Sez.~\ref{app:canali}).

L'\textbf{Appendice~\ref{app:strumentiecodice}} (\textit{Strumenti, Ambiente e Codice}) motiva in dettaglio le scelte tecnologiche --- confronto tra framework, metodi di simulazione di Aer, specifiche GPU e ambiente WSL (Sez.~\ref{app:strumenti}) --- e riporta il codice sorgente dei componenti implementati (Sez.~\ref{app:codice}), dal circuito di Shor ai codici correttori.

L'\textbf{Appendice~\ref{app:contesto}} (\textit{Contesto e Motivazione}) sviluppa i principi fisici del calcolo quantistico, la rivoluzione algoritmica degli anni Novanta, il protocollo RSA e la sua vulnerabilità, la crittografia post-quantistica e lo stato dell'arte dell'hardware (Sez.~\ref{app:introduzione}); raccoglie inoltre la motivazione crittografica estesa, i requisiti funzionali e la specifica completa di input e output del sistema sperimentale (Sez.~\ref{app:obiettivi}).

L'\textbf{Appendice~\ref{app:fase1}} (\textit{Fase 1: la Campagna Classica nel Dettaglio}) riporta integralmente gli otto sweep parametrici su $M_{\text{TOP4}}$, con il confronto previsione--esito per ciascun parametro, e il dettaglio metodologico del confronto con la Zero-Noise Extrapolation (Sez.~\ref{app:parametrica}); documenta poi il classificatore valutato nella prima fase --- le tre architetture confrontate, l'addestramento e le metriche --- e l'audit delle etichette che ricostruisce quale domanda il modello abbia effettivamente appreso a rispondere (Sez.~\ref{app:classificatore}).

L'\textbf{Appendice~\ref{app:fase2}} (\textit{Fase 2: la Campagna QEC nel Dettaglio}) espone per intero la campagna sulla decodifica appresa --- architettura del decoder, protocollo di addestramento, confronto con \ac{MWPM} e con BP+OSD (Sez.~\ref{app:decodifica}) --- e mette alla prova il criterio conclusivo del lavoro su un caso esterno al perimetro principale, la mappatura del circuito studiata su una snapshot di calibrazione offline e riproducibile (Sez.~\ref{app:compilazione}).

\end{onehalfspace}
