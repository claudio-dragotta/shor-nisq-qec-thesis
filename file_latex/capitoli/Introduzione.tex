\begin{onehalfspace}

\section{Il Limite del Calcolo Classico}

Per oltre cinquant'anni, lo sviluppo dell'informatica è stato guidato da una legge empirica enunciata nel 1965 da Gordon Moore\footnote{\textbf{Gordon Earle Moore} (1929--2023), cofondatore di Intel, osservò nel 1965 che il numero di transistor integrabili su un chip raddoppiava approssimativamente ogni due anni, con un conseguente aumento esponenziale delle prestazioni computazionali. La Legge di Moore non è una legge fisica, ma una previsione industriale che ha orientato decenni di investimenti tecnologici.}: il numero di transistor per chip raddoppia ogni due anni, e con esso la potenza di calcolo disponibile. Questa crescita esponenziale ha permesso di trasformare calcolatori grandi quanto un'intera stanza nei dispositivi tascabili di oggi. Tuttavia, la Legge di Moore si sta scontrando con i limiti fisici fondamentali della materia: i transistor moderni hanno dimensioni di pochi nanometri, avvicinandosi alla scala atomica oltre la quale gli effetti quantistici -- lo stesso fenomeno che si vorrebbe evitare -- diventano incontrollabili e inevitabili.

Parallelamente al problema fisico dell'hardware, esiste una barriera di natura puramente matematica: esistono classi di problemi computazionali per i quali nessun algoritmo classico è in grado di fornire una soluzione in tempi ragionevoli, \textit{indipendentemente} dalla potenza di calcolo disponibile. Questi problemi -- fattorizzazione di grandi interi, ricerca in spazi combinatoriali enormi, simulazione di sistemi molecolari complessi -- resistono a qualunque approccio classico non perché i computer non siano abbastanza veloci, ma perché la complessità intrinseca del problema cresce più rapidamente di qualunque risorsa classica concepibile.

È in questo contesto che nasce il \textbf{calcolo quantistico}: la visione radicale che i principi della meccanica quantistica -- \textit{sovrapposizione}, \textit{entanglement} e \textit{interferenza} -- possano essere sfruttati non come ostacoli da evitare, ma come risorse computazionali di straordinaria potenza. L'intuizione originale fu di Richard Feynman\footnote{\textbf{Richard Phillips Feynman} (1918--1988), fisico teorico statunitense, Premio Nobel per la Fisica nel 1965. Nel suo celebre articolo del 1982 \textit{``Simulating Physics with Computers''} \cite{feynman1982}, Feynman argomentò che un computer classico non può simulare efficientemente un sistema quantistico senza un overhead esponenziale, e propose che un ``computer quantistico'' -- uno che segua le leggi della meccanica quantistica -- potrebbe farlo in modo naturalmente efficiente.}, che nel 1982 osservò come la simulazione di sistemi quantistici richiedesse risorse esponenziali su macchine classiche, e propose che un computer basato sugli stessi principi fisici potesse simulare la natura stessa in modo efficiente \cite{feynman1982}. David Deutsch formalizzò nel 1985 il primo modello teorico rigoroso di computer quantistico universale \cite{deutsch1985}, aprendo la strada a un nuovo paradigma computazionale.

I principi fisici che rendono possibile questo paradigma --- il qubit e la sovrapposizione, la sfera di Bloch, l'entanglement e l'interferenza --- insieme alla loro realizzazione sui qubit superconduttori di IBM, sono ripresi nell'Appendice~\ref{app:introduzione} (Sez.~\ref{app:intro:qubit}); la trattazione formale completa è nel Capitolo~\ref{chap:fondamenti}. La rivoluzione algoritmica degli anni Novanta --- da Simon fino all'algoritmo di Shor (1994) e a quello di Grover (1996) --- è ripercorsa nella Sez.~\ref{app:intro:algoritmi}. La portata dirompente di Shor si misura sulla crittografia che minaccia: la struttura del protocollo RSA e il motivo per cui Shor lo rompe (Sez.~\ref{app:intro:rsa}), la risposta della crittografia post-quantistica con gli standard NIST 2024 (Sez.~\ref{app:intro:pqc}) e lo stato dell'arte dell'hardware dall'era NISQ verso i sistemi fault-tolerant (Sez.~\ref{app:intro:nisq}) delineano il contesto strategico in cui si colloca questo lavoro: comprendere i limiti reali dell'algoritmo di Shor su hardware rumoroso.
% -----------------------------------------------
\section{Motivazione e Contributo del Presente Lavoro}

In questo contesto scientifico e tecnologico si inserisce il presente lavoro di tesi. L'algoritmo di Shor rappresenta uno dei risultati più significativi e dirompenti dell'informatica quantistica: la sua capacità di fattorizzare interi in tempo polinomiale minaccia direttamente le fondamenta della crittografia moderna. Tuttavia, la sua esecuzione su hardware NISQ è ostacolata da requisiti stringenti in termini di profondità del circuito, numero di qubit e fedeltà delle porte -- rendendo il rumore quantistico il principale ostacolo alla sua realizzazione pratica.

Questo lavoro affronta tale sfida in due fasi. La \textbf{prima fase} è una verifica sperimentale sistematica: eseguire l'algoritmo di Shor su simulatori quantistici con noise model calibrati sull'hardware IBM e mettere alla prova l'ipotesi che un classificatore \ac{ML} applicato all'output rumoroso riduca il numero di iterazioni necessarie. La verifica confronta tre configurazioni:
\begin{itemize}
    \item \textbf{Metodo 1} (baseline classico, TOP-1): la misura più frequente dell'istogramma viene usata come candidato. Richiede in media $\bar{M}_1 = 1.97$ esecuzioni per $N=15$ in condizioni NISQ-realistiche.
    \item \textbf{Metodo 2} (classificatore \ac{ML} + TOP-4): un classificatore binario (Random Forest) pre-addestrato su 2\,000 istogrammi sintetici filtra le iterazioni che giudica prive di segnale \ac{QPE} recuperabile; la ricerca è poi estesa ai 4 candidati più frequenti. Ottiene $\bar{M}_2 = 1.03$, con fattore di riduzione $\rho = 1.90$.
    \item \textbf{Analisi di ablazione} ($M_{\text{TOP4}}$, TOP-4 senza classificatore): isola il contributo della ricerca multi-candidato da quello del filtro ML. L'ablazione mostra che $M_{\text{TOP4}}$ ottiene $\bar{M} = 1.00$ --- ogni ripetizione converge al primo tentativo --- ed è statisticamente indistinguibile da M2 per UC1 ($p = 0.849$), mentre su UC2 il classificatore richiede significativamente più iterazioni della sola ricerca: il guadagno è attribuibile alla \textbf{strategia di ricerca TOP-4}, non al classificatore. Un audit successivo delle etichette di addestramento (Appendice~\ref{app:classificatore}) ne individua la causa meccanica: il classificatore era stato addestrato, di fatto, a riconoscere se il picco più alto dell'istogramma coincidesse con l'esito banale $y=0$ --- ossia proprio il caso che la ricerca multi-candidato risolve per costruzione.
\end{itemize}

L'esito della verifica --- l'\ac{ML} applicato all'output, in quel punto e in quel modo, non serve --- delimita con precisione dove va cercata la soluzione: se l'informazione distrutta dal rumore non è recuperabile a valle, l'errore va corretto \emph{durante} il calcolo. La \textbf{seconda fase}, che costituisce il nucleo della tesi, è quindi dedicata alla \ac{QEC}: la costruzione e la simulazione dei codici correttori --- dal codice a ripetizione al codice di Steane $[[7,1,3]]$, fino al surface code con decodifica \ac{MWPM} --- e la misura sperimentale del \textbf{logical error rate} $p_L$ in funzione dell'errore fisico $p$ e della distanza di codice $d$. L'integrazione finale avviene a livelli: lo \textbf{Shor logico}, in cui ogni gate del circuito subisce l'errore logico residuo $p_L$ misurato sperimentalmente, risponde alla domanda conclusiva del lavoro: \emph{quale livello di correzione è necessario perché Shor mantenga una probabilità di successo accettabile?}

È in questa seconda fase che l'intelligenza artificiale viene ripresa, e nel punto in cui l'esito della prima indica che possa servire: non più sull'output, dove l'informazione sull'errore è già andata perduta, ma sulla \textbf{sindrome}, dove è ancora interamente presente. Un decoder appreso viene messo a confronto con il \ac{MWPM} sugli stessi campioni, e il risultato precisa il criterio che regge l'intero lavoro. Sostituire il decoder analitico non conviene: a parità di modello di rumore la rete lo pareggia soltanto, e a costo di volumi di dati considerevoli. Affiancarglielo sì: un decoder \textbf{ibrido}, in cui la rete apprende unicamente \emph{quando ribaltare} la decisione del matching, riduce l'errore logico fino al $21\%$ quando il dispositivo presenta errori correlati che il decoder analitico non è in grado di rappresentare --- e ricade esattamente su di esso quando il rumore è quello previsto, o quando il codice cresce al punto da lasciare al modello appreso troppo pochi esempi da cui imparare. L'apprendimento automatico, dunque, non ottimizza l'esecuzione di Shor ovunque lo si applichi: lo fa dove dispone di informazione che nessun metodo più semplice sa sfruttare, e questa tesi ne individua sperimentalmente il confine.

L'elaborato si sviluppa dalle fondamenta teoriche del calcolo quantistico, attraverso la verifica sperimentale su simulatori rumorosi, fino alla correzione d'errore quantistica e allo Shor logico. Gli obiettivi specifici e la struttura dettagliata del lavoro sono descritti nel Capitolo~\ref{chap:obiettivi}.

\medskip
\noindent\textbf{Riproducibilità e materiale supplementare.} Tutto il lavoro condotto in questa tesi --- i sorgenti \LaTeX{}, il codice sperimentale e le campagne di simulazione con i relativi risultati (seed e parametri inclusi) --- è documentato ed è liberamente consultabile nel repository \textsc{GitHub} pubblico dell'autore: \url{https://github.com/claudio-dragotta/shor-nisq-qec-thesis}. È inoltre disponibile una \textbf{demo interattiva} dell'algoritmo di Shor --- circuito e sfere di Bloch passo per passo, con esecuzione statistica su molte iterazioni --- consultabile dal vivo all'indirizzo \url{https://shor-demo-6knp.onrender.com} (codice sorgente: \url{https://github.com/claudio-dragotta/shor-demo}).

\clearpage
% ⚠ SYNC-STRUTTURA: questa sezione deve essere aggiornata ogni volta che
%   si aggiunge, elimina o rinomina un capitolo. Quando modifichi un capitolo,
%   controlla se la sua descrizione qui è ancora accurata.
\section{Struttura della Tesi}

L'elaborato è organizzato in quindici capitoli, seguiti da sette appendici (A--G) che raccolgono il contesto esteso, il materiale di dettaglio richiamato in forma sintetica nel corpo e un'esplorazione condotta oltre il perimetro del lavoro principale. Il Capitolo~2 svolge una funzione trasversale di raccordo tra teoria e pratica; i Capitoli~3--6 costituiscono la parte teorica; i Capitoli~7--13 la parte sperimentale; i Capitoli~14 e~15 chiudono il lavoro.

\medskip
Il \textbf{Capitolo~\ref{chap:obiettivi}} (\textit{Obiettivi e Piano Sperimentale}) motiva la vulnerabilità di RSA all'algoritmo di Shor e fissa il contratto sperimentale: obiettivi, ipotesi di ricerca, contributo, use case e metriche.

\medskip
\textbf{Parte teorica (Capitoli 3--6).}

Il \textbf{Capitolo~\ref{chap:fondamenti}} (\textit{Fondamenti del Calcolo Quantistico}) richiama qubit, sovrapposizione, porte e circuiti, e l'entanglement come \textit{risorsa fisica fragile} che anticipa la trattazione del rumore.

Il \textbf{Capitolo~\ref{chap:shor}} (\textit{L'Algoritmo di Shor}) presenta la riduzione al \textit{period finding}, la Quantum Phase Estimation e la complessità polinomiale $O((\log N)^3)$, con le implicazioni per RSA e la transizione post-quantum.

Il \textbf{Capitolo~\ref{chap:rumore}} (\textit{Rumore Quantistico nei Sistemi NISQ}) analizza le quattro fonti fisiche di errore e i canali di rumore fondamentali, fino al crollo della probabilità di successo (${\sim}3.4\%$) già per un modulo di soli 15 bit.

Il \textbf{Capitolo~\ref{chap:strategie}} (\textit{Strategie per la Riduzione del Rumore Quantistico}) confronta le quattro macro-strategie anti-rumore nell'era NISQ e motiva l'approccio adottato.

\medskip
\textbf{Parte sperimentale (Capitoli 7--14).}

Il \textbf{Capitolo~\ref{chap:strumenti}} (\textit{Strumenti e Ambiente di Simulazione}) descrive Qiskit, il simulatore Aer con GPU, il noise model parametrizzato e la transpilazione.

Il \textbf{Capitolo~\ref{chap:metodologia}} (\textit{Metodologia e Architettura}) presenta la pipeline sperimentale a tre stadi, le tecniche di mitigazione del rumore e i due metodi confrontati.

Il \textbf{Capitolo~\ref{chap:sviluppo}} (\textit{Sviluppo e Implementazione}) descrive l'implementazione: ambiente WSL2, QFT inversa, circuito di Shor, noise model e pipeline dei due metodi.

Il \textbf{Capitolo~\ref{chap:risultati}} (\textit{Verifica Sperimentale dell'Ipotesi Iniziale: dal Classificatore ML alla Strategia TOP-K}) stabilisce la baseline e, tramite analisi di ablazione, dimostra che il guadagno è della strategia TOP-4 e non del classificatore \ac{ML} --- il risultato che motiva il pivot alla QEC.

Il \textbf{Capitolo~\ref{chap:qec}} (\textit{La Correzione d'Errore Quantistico: dal Codice a Ripetizione al Codice di Steane}) presenta il ciclo di correzione e lo verifica sul codice a ripetizione e sul codice di Steane $[[7,1,3]]$.

Il \textbf{Capitolo~\ref{chap:surface}} (\textit{Il Surface Code e la Stima dell'Errore Logico}) estende la correzione al surface code e ne misura sperimentalmente l'errore logico e la soglia con Stim e PyMatching; le due sezioni conclusive mettono alla prova le assunzioni su cui quella stima poggia --- il modello di rumore con cui il codice è simulato e quello con cui è decodificato --- ed è nella seconda che il confronto fra decodifica appresa e \ac{MWPM} individua il regime in cui l'apprendimento automatico produce un guadagno misurabile.

Il \textbf{Capitolo~\ref{chap:integrazione}} (\textit{Integrazione: lo Shor Logico e i Requisiti di Correzione}) esegue lo Shor logico con l'errore residuo misurato e traduce il livello di correzione necessario in requisiti di hardware.

Il \textbf{Capitolo~\ref{chap:sviluppi}} (\textit{Sviluppi Futuri}) delinea le direzioni di ricerca oltre il perimetro sperimentale della tesi.

Il \textbf{Capitolo~\ref{chap:conclusioni}} (\textit{Conclusioni}) verifica una per una le sei ipotesi e le sei domande di ricerca fissate nel Capitolo~\ref{chap:obiettivi}, dichiara i contributi e i limiti del lavoro e isola il criterio che unifica le due fasi sperimentali.

\medskip
\textbf{Appendici.} L'\textbf{Appendice~\ref{app:parametrica}} (\textit{Campagna Parametrica Dettagliata e Confronto ZNE}) riporta integralmente le sette sweep parametriche su $M_{\text{TOP4}}$, con il confronto previsione--esito per ciascun parametro, e il dettaglio metodologico del confronto con la Zero-Noise Extrapolation.

L'\textbf{Appendice~\ref{app:fondamenti}} (\textit{Fondamenti del Calcolo Quantistico --- Trattazione Estesa}) riporta per esteso i postulati della meccanica quantistica, l'insieme completo delle porte, le figure e il modello di complessità sintetizzati nel Capitolo~\ref{chap:fondamenti}.

L'\textbf{Appendice~\ref{app:strumenti}} (\textit{Strumenti e Ambiente di Simulazione --- Trattazione Estesa}) motiva in dettaglio le scelte tecnologiche --- confronto tra framework, metodi di simulazione di Aer, specifiche GPU e ambiente WSL --- sintetizzate nel Capitolo~\ref{chap:strumenti}.

L'\textbf{Appendice~\ref{app:introduzione}} (\textit{Contesto Esteso dell'Introduzione}) sviluppa i principi fisici del calcolo quantistico, la rivoluzione algoritmica degli anni Novanta, il protocollo RSA e la sua vulnerabilità, la crittografia post-quantistica e lo stato dell'arte dell'hardware quantistico.

L'\textbf{Appendice~\ref{app:obiettivi}} (\textit{Dettaglio degli Obiettivi e del Piano Sperimentale}) raccoglie la motivazione crittografica estesa, i requisiti funzionali del sistema sperimentale e la specifica completa dei parametri di input e di output.

L'\textbf{Appendice~\ref{app:classificatore}} (\textit{Il Classificatore ML: Architettura, Addestramento e Metriche}) riporta il dettaglio del classificatore valutato nella prima fase sperimentale --- le tre architetture confrontate, l'addestramento e le metriche di classificazione --- il cui verdetto operativo è discusso nel Capitolo~\ref{chap:risultati}; contiene inoltre l'audit delle etichette di addestramento, che ricostruisce quale domanda il modello abbia effettivamente appreso a rispondere.

L'\textbf{Appendice~\ref{app:compilazione}} (\textit{Compilazione Consapevole della Struttura: un'Esplorazione}) mette alla prova il criterio conclusivo del lavoro su un caso esterno al perimetro principale --- la scelta della mappatura del circuito sui qubit fisici, condotta su dati di calibrazione reali --- e ne ricava una precisazione.

\end{onehalfspace}
