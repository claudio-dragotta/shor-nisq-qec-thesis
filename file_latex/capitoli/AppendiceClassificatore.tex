\begin{onehalfspace}

Questa appendice riporta il dettaglio del classificatore \ac{ML} valutato nella prima fase sperimentale (Capitolo~\ref{chap:risultati}): architettura, procedura di addestramento, metriche di classificazione e --- nella Sezione~\ref{sec:audit_etichette} --- l'audit che ne ha chiarito il comportamento. Il verdetto operativo, ossia che il classificatore a valle dell'esecuzione non riduce le iterazioni rispetto alla sola ricerca multi-candidato TOP-4, è dimostrato nel corpo del capitolo tramite l'analisi di ablazione; qui se ne ricostruisce la causa.

\section{L'Ipotesi alla Prova: il Classificatore ML}
\label{sec:risultati_uc_m2}
\label{sec:valutazione_clf}

L'ipotesi formalizzata nel Capitolo~\ref{chap:obiettivi} era che un classificatore \ac{ML}, addestrato a riconoscere gli istogrammi privi di segnale QPE, riducesse le iterazioni scartando le esecuzioni inutili: quantitativamente, $\bar{M}_2 \approx 3$ e $\rho = \bar{M}_1/\bar{M}_2 > 1$ con $p < 0.05$ (Mann-Whitney). Il disegno sperimentale include fin dall'inizio la variante di ablazione $M_{\text{TOP4}}$: poiché il Metodo 2 combina due componenti (filtro \ac{ML} + ricerca TOP-4), solo il confronto a tre permette di attribuire un eventuale guadagno all'una o all'altra.

\paragraph{Architettura e addestramento.}
Il Metodo 2 opera su due livelli: un \textit{gate di qualità} --- il classificatore riceve il vettore di distribuzione normalizzato $\mathbf{x} \in \mathbb{R}^{256}$ e predice se l'istogramma contiene segnale recuperabile --- e una \textit{ricerca multi-candidato}: se il classificatore approva, si tentano i 4 candidati più frequenti. Per ciascun use case è stato generato un dataset di 2000 campioni con noise model variato del $\pm50\%$ intorno al valore nominale, ottenendo il $74.2\%$ di positivi per UC1 e l'$83.2\%$ per UC2. Tre architetture sono state confrontate --- Random Forest, \ac{SVM} con kernel RBF, \ac{MLP} $(256,128)$ --- con split 80/20 stratificato e selezione per F1-score.

\begin{table}[H]
\centering
\caption{Metriche dei classificatori sul test set (20\% del dataset)}
\label{tab:clf_metrics}
\begin{tabular}{llccc}
\hline
\textbf{Use Case} & \textbf{Modello} & \textbf{F1} & \textbf{Accuratezza} & \textbf{\acs{AUC}-ROC} \\
\hline
\multirow{3}{*}{UC1} & \textbf{Random Forest (selezionato)} & \textbf{0.949} & \textbf{0.922} & \textbf{0.964} \\
                     & \ac{SVM}                             & 0.855          & 0.748          & 0.929 \\
                     & \ac{MLP}                             & 0.919          & 0.885          & 0.952 \\
\hline
\multirow{3}{*}{UC2} & \textbf{\ac{SVM} (selezionato)}      & \textbf{0.979} & \textbf{0.965} & \textbf{0.987} \\
                     & Random Forest                        & 0.976          & 0.960          & 0.982 \\
                     & \ac{MLP}                             & 0.977          & 0.963          & 0.987 \\
\hline
\end{tabular}
\end{table}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{fig_roc_curve.pdf}
    \caption[Curve ROC dei classificatori selezionati (UC1: Random Forest, UC2: \acs{SVM})]{Curve ROC dei classificatori selezionati: Random Forest per UC1 (\acs{AUC}=0.964) e \acs{SVM} per UC2 (\acs{AUC}=0.987). La linea tratteggiata rappresenta il classificatore casuale (\acs{AUC}=0.5).}
    \label{fig:roc_curve}
\end{figure}

Come \emph{classificatori}, i modelli funzionano molto bene: F1 fino a $0.98$ e \ac{AUC} fino a $0.99$. Ma le metriche di classificazione dicono soltanto che il modello sa distinguere le due classi che gli sono state presentate, e non dicono nulla su \emph{quali} classi siano. L'ablazione del Capitolo~\ref{chap:risultati} ha mostrato che il filtraggio non riduce le iterazioni rispetto alla sola ricerca multi-candidato; la sezione seguente stabilisce perché.

% -----------------------------------------------
\section{Audit delle Etichette: che Cosa il Classificatore ha Effettivamente Appreso}
\label{sec:audit_etichette}

La coesistenza di metriche eccellenti e di un guadagno operativo nullo è sufficientemente anomala da richiedere una verifica diretta, anziché una spiegazione narrativa. Poiché i modelli addestrati sono stati archiviati assieme al proprio test set, è possibile \emph{ricalcolare} le etichette a partire dagli istogrammi salvati e confrontarle con quelle memorizzate. È quanto fa lo script \texttt{audit\_etichette\_classificatore.py}, il cui esito è riportato nella Tabella~\ref{tab:audit_etichette}.

La regola di etichettatura documentata prevede $\text{TOP\_K} = 16$: un istogramma è positivo se almeno una delle sedici misure più frequenti conduce ai fattori corretti.

\begin{table}[H]
\centering
\caption[Audit delle etichette del dataset di addestramento]{Concordanza fra le etichette memorizzate nel test set e quelle ricalcolate dagli istogrammi con diverse regole di etichettatura. La regola documentata è TOP-16; le etichette effettive corrispondono a TOP-1.}
\label{tab:audit_etichette}
\begin{tabular}{lcc}
\hline
\textbf{Regola usata per il ricalcolo} & \textbf{UC1} & \textbf{UC2} \\
\hline
TOP-1 (la sola moda)          & \textbf{399/400 (99.8\%)} & \textbf{396/400 (99.0\%)} \\
TOP-2                         & 297/400 (74.2\%)          & 333/400 (83.2\%) \\
TOP-4                         & 297/400 (74.2\%)          & 333/400 (83.2\%) \\
TOP-16 (regola documentata)   & 297/400 (74.2\%)          & 333/400 (83.2\%) \\
\hline
\end{tabular}
\end{table}

Le etichette effettivamente usate per l'addestramento coincidono con la regola TOP-1, non con TOP-16: la concordanza è del $99.8\%$ contro il $74.2\%$ per UC1, e del $99.0\%$ contro l'$83.2\%$ per UC2 --- dove il valore più basso non è altro che la frequenza di base della classe positiva, cioè quanto si otterrebbe etichettando tutto come positivo. Il parametro è stato evidentemente modificato nel codice dopo la generazione del dataset, senza rigenerarlo.

La conseguenza va enunciata con precisione, perché non è quella che il nome della variabile suggerisce. Poiché fra i quattro picchi della \ac{QPE} l'unico che non conduce ai fattori è l'esito banale $y=0$ (fase nulla, scartata per costruzione dalla funzione di estrazione), etichettare con TOP-1 equivale a etichettare con la domanda: \emph{la moda dell'istogramma cade sulla cella $0$?} La verifica lo conferma senza eccezioni: dei $103$ campioni negativi di UC1 tutti e $103$ hanno moda $y=0$, e così tutti e $67$ quelli di UC2.

\begin{table}[H]
\centering
\caption[Struttura degli istogrammi etichettati come negativi]{Struttura degli istogrammi della classe negativa. Su UC1 essi conservano il segnale \ac{QPE} praticamente intatto: la differenza rispetto ai positivi non è la presenza del segnale, ma quale dei quattro picchi risulti il più alto.}
\label{tab:struttura_negativi}
\begin{tabular}{lcc}
\hline
 & \textbf{UC1} & \textbf{UC2} \\
\hline
Massa media sui quattro picchi $\{0, 64, 128, 192\}$ & 0.758 & 0.294 \\
Entropia media dell'istogramma (uniforme $= 8$ bit)  & 3.66 bit & 6.37 bit \\
Campioni negativi con moda $y=0$                      & 103/103 & 67/67 \\
\hline
\end{tabular}
\end{table}

Per UC1 il dato della Tabella~\ref{tab:struttura_negativi} è dirimente: gli istogrammi ``negativi'' concentrano il $75.8\%$ della propria massa sui quattro picchi della \ac{QPE} e hanno entropia $3.66$ bit su $8$ --- valori indistinguibili da quelli della classe positiva. Non sono affatto istogrammi privi di segnale: sono istogrammi in cui il segnale c'è tutto, e in cui il picco più alto è quello privo di contenuto informativo. In un campione tipico della classe negativa, il secondo, terzo e quarto valore per frequenza sono esattamente $192$, $128$ e $64$, e ciascuno dei tre conduce alla fattorizzazione corretta $(3,5)$.

\paragraph{Perché questo spiega tutti e tre gli esiti osservati.}
L'audit ricompone in un'unica causa i tre fatti che il Capitolo~\ref{chap:risultati} registra separatamente.

\begin{itemize}
    \item \textbf{Le metriche sono eccellenti perché il compito è quasi banale.} Predire se l'argmax di un vettore cade su una cella prefissata è una funzione elementare dell'input, non il riconoscimento di una firma di rumore. F1 $=0.95$ misura la capacità del modello di individuare il massimo di $256$ numeri, non la sua capacità di diagnosticare la corruzione dell'informazione quantistica.
    \item \textbf{Il contributo operativo è nullo perché la ricerca TOP-4 copre già quel caso.} Quando la moda è $y=0$ i picchi utili occupano le posizioni successive, e TOP-4 li raggiunge per costruzione. Il classificatore scarta l'iterazione; la ricerca multi-candidato la risolve. I due componenti agiscono sullo stesso identico insieme di esecuzioni, ed è per questo che l'ablazione su UC1 non rileva differenza significativa ($p = 0.849$).
    \item \textbf{Su UC2 il filtro diventa dannoso per la stessa ragione.} Scartare un'iterazione recuperabile ha un costo: bisogna eseguirne un'altra. È esattamente ciò che si osserva, con $\bar{M}$ che risale da $1.00$ a $1.57$ ($p < 0.001$).
\end{itemize}

\paragraph{Il problema di classificazione, così come documentato, non esiste.}
Resta da chiedersi che cosa accadrebbe addestrando il classificatore sulla regola dichiarata anziché su quella applicata. La rigenerazione del dataset di UC1 con $\text{TOP\_K} = 16$, a parità di ogni altro parametro, produce \textbf{2000 campioni positivi su 2000}: la classe negativa è vuota e la procedura di addestramento si interrompe segnalando l'assenza di una seconda classe. Il risultato non è un artefatto dello schema di seeding --- la rigenerazione ripetuta con il seeding originale dà lo stesso esito --- né della versione del transpiler, verificata riportando il livello di rumore a quello che riproduce la profondità efficace precedente.

L'interpretazione è la seguente: al livello di rumore di UC1, e con una ricerca che consideri anche solo i primi sedici candidati, \emph{non esiste alcun istogramma da scartare}. La domanda a cui il classificatore avrebbe dovuto rispondere non ha istanze negative, e quindi non è una domanda. Il filtro a valle non è ridondante per una qualche insufficienza dell'architettura scelta o della quantità di dati: è ridondante perché il rumore depolarizzante, su questo circuito e a questa profondità, non produce mai la condizione che esso dovrebbe rilevare.

\subsection{A quale livello di rumore la classe negativa comincia a esistere}
\label{subsec:soglia_classe_negativa}

Constatare che la classe negativa è vuota a un dato livello di rumore lascia aperta la domanda su \emph{dove} cominci a popolarsi: è quella soglia a delimitare il regime in cui un filtro a valle avrebbe qualcosa da apprendere. La si può stimare per via analitica e misurare direttamente, e le due strade convergono.

\paragraph{Previsione.} Con $1\,024$ shot distribuiti su $256$ celle, una cella di fondo riceve in media $4(1-f)$ conteggi, dove $f$ è la frazione coerente efficace introdotta nella Sezione~\ref{subsec:frazione_coerente}; un picco \ac{QPE} ne riceve $256f + 4(1-f)$. Perché un picco entri fra i sedici valori più frequenti deve superare la sedicesima cella di fondo, il cui conteggio è il quantile superiore al $6.3\%$ di una Poisson di media $4$, ossia $8$. Imponendo $256f + 4(1-f) \geq 8$ si ottiene
\begin{equation}
    f \gtrsim 1.6\% ,
    \label{eq:soglia_f}
\end{equation}
sotto il quale i picchi utili escono dai primi sedici candidati e l'istogramma diventa genuinamente inutilizzabile.

\paragraph{Misura.} La Tabella~\ref{tab:soglia_negativi} riporta la frazione di campioni negativi su $200$ istogrammi per punto, con $\varepsilon_{2q}$ spinto ben oltre qualunque valore fisicamente sensato.

\begin{table}[H]
\centering
\caption[Soglia di comparsa della classe negativa]{Frazione di istogrammi privi di segnale recuperabile nei primi sedici candidati, in funzione di $\varepsilon_{2q}$ ($200$ campioni per punto, regola TOP-16). La colonna $f$ riporta la frazione coerente efficace misurata indipendentemente (Sezione~\ref{subsec:frazione_coerente}).}
\label{tab:soglia_negativi}
\begin{tabular}{crrrr}
\hline
$\varepsilon_{2q}$ & rispetto a UC1 & $P_{\text{surv}}$ & $f$ & negativi \\
\hline
$5\times10^{-2}$ (UC2) & $5\times$  & $2.0\times10^{-4}$  & 0.372 & \phantom{00}0.0\% \\
$8\times10^{-2}$       & $8\times$  & $9.7\times10^{-7}$  & ---   & \phantom{00}0.0\% \\
$10^{-1}$              & $10\times$ & $2.5\times10^{-8}$  & 0.158 & \phantom{00}0.0\% \\
$1.5\times10^{-1}$     & $15\times$ & $1.9\times10^{-12}$ & ---   & \phantom{00}0.0\% \\
$2\times10^{-1}$       & $20\times$ & $8.2\times10^{-17}$ & 0.032 & \phantom{00}0.5\% \\
\hline
\end{tabular}
\end{table}

Il primo campione negativo compare a $\varepsilon_{2q} = 2\times10^{-1}$, ossia a \textbf{venti volte} il tasso di errore a due qubit dei dispositivi superconduttori attuali, e in quel punto la frazione coerente efficace misurata vale $3.2\%$ --- appena sopra la soglia $1.6\%$ prevista dalla~\eqref{eq:soglia_f}. Le due stime, ricavate da misure indipendenti, concordano entro un fattore due.

\paragraph{Conclusione sul perimetro di validità.} Ne discende l'enunciato più preciso che questi dati consentono, ed è più forte di quello ricavato dal solo UC1. Il problema di classificazione su cui la prima fase della tesi ha investito \emph{non esiste in nessuno dei due use case previsti}: non a UC1, e neppure a UC2, che pure adotta un livello di rumore cinque volte superiore. Perché esso cominci ad avere istanze negative --- e dunque perché un filtro a valle abbia qualcosa da imparare --- occorre un errore di gate a due qubit venti volte peggiore di quello degli \ac{QPU} odierni, un regime in cui nessun algoritmo quantistico sarebbe comunque eseguibile. Il filtro applicato all'output non è quindi soltanto inutile ai livelli di rumore attuali: è \emph{mal posto per costruzione} su questo compito, perché la condizione che dovrebbe rilevare e il regime in cui l'algoritmo funziona sono insiemi disgiunti.

È il risultato negativo nella sua forma più netta, e delimita con precisione il regime in cui vale: non ``l'apprendimento automatico non serve alla mitigazione degli errori quantistici'', bensì \emph{un filtro applicato a valle di un output già ricco di struttura ridondante non ha nulla da apprendere che la struttura non fornisca già gratuitamente}. È esattamente questa constatazione a motivare lo spostamento della seconda fase del lavoro sulla sindrome, dove l'informazione è disponibile \emph{durante} il calcolo e non dopo (Capitoli~\ref{chap:qec}--\ref{chap:integrazione}, e in particolare la decodifica appresa della Sezione~\ref{sec:decodifica_appresa}).

\end{onehalfspace}
